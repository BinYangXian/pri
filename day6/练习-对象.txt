练习：
2、类创建对象
电脑类

属性：
          型号（String类型）
          显示器（自定义新的类型（class类））
          主机（自定义新的类型（class类））
          键盘（String类型）
          鼠标（String类型）
方法：
        打游戏（实现逻辑：主机进行数据运算，显示器进行内容显示）

显示器类

属性：
     型号（String类型）
     大小（int类型）

方法：
    显示内容

主机类

属性：
    中央处理器型号（String类型）
    图形处理器型号（String类型）
 方法：
    数据运算

a）创建一个mac pro电脑，设置显示器为（型号：lg a1，大小：29寸），用mac pro进行打游戏
b）创建一个thinkpad x200电脑，设置显示器为（型号：sony s2，大小：27寸），用thinkpad x200进行打游戏
c）使用mac pro电脑的显示器的显示功能，并为其显示器设置新名字
d）使用thinkpad x200电脑的显示器显示功能，并打印其显示器的名字