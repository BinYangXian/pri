练习三
一、问答题
    1. 按你自己的想法，为什么需要数组这个概念?
    2. 请描述并对比数组的初始化方式?
    3. 数组的长度可变吗，什么时候确定长度?
    4. 数组的复制用哪个类的方法实现较好?
    5. 说明Arrays类的作用?

二、编程题（数组api练习，根据api解决如下问题）
1、创建一个int数组a1一共有10个元素
完成如下需求：
    1）a1里所有元素赋值初始值都为5
    2）再把a1所有元素的值设置为（其初始值乘以下标）
    3）把a1数组存放的值倒序存储-例如：{1,3,2,4}->{4,2,3,1}
    4）把数组a1存放的值按大小升序排列
    5）把数组a1里第2-8个元素copy到新数组a2
    6）把数组a1里所有元素copy到新数组a3
    7）打印出a1，a2，a3的内容
    8）查找值30在a2中的下标。
